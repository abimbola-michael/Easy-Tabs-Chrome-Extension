export enum AppMode {
  createTab,
  createTabGroup,
  createCategory,
  modifyCategory,
  deleteTab,
  deleteTabGroup,
}

export enum TabMode {
  grid,
  list,
}

export enum ListControlMode {
  filter,
  sort,
  group,
  search,
}

export enum PopupControlType {
  hover,
  leftClick,
  rightClick,
}
